{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "start": "node server.js",
    "build": "esbuild ./*.js --bundle --platform=node --outdir=dist --minify",
    "dev": "esbuild ./*.js --bundle --platform=node --outdir=dist --watch",
    "dev": "esbuild ./*.js --bundle --platform=node --outdir=dist --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "eslint": "^9.38.0",
    "globals": "^16.4.0",
    "esbuild": "^0.20.0",
    "jest": "^29.7.0"
  }
}
